import React from "react";

export type benefitsDataType = {
  icon: JSX.Element;
  title: string;
  description: string;
};

export type ClassType = {
  name: string;
  description?: string;
  image: string;
};
